---
title: 'API Endpoints'
description: 'Explore the available WetroCloud API endpoints for creating, managing, querying, and categorizing collections, with detailed response formats for seamless integration.'
---
### Endpoints
||                                                        |
|---------------------|-----------------------------------------------------------------------------|
| **`/create`**  | Used to create a new collection where your resources will be organized.     |
| **`/insert`**  | Allows you to add resources, such as documents or files, into a specified collection. |
| **`/query`**     | Enables you to search or retrieve information from your collections based on specific prompts. |
| **`/delete`**   | Removes an entire collection and its associated resources.                 |
| **`/category`** | Categorizes your data according to specific rules or schema definitions. |
### Response Formats by Endpoint
The WetroCloud API returns responses in a consistent JSON format. Each endpoint includes fields relevant to the specific operation.

#### Create Endpoint (`/create`)

Used to create a new collection. The response includes the unique ID of the collection and a success status.
<Frame>
  <img
    src="/images/postman-images/Create-collection.png"
    alt="Postmandocs"
  />
</Frame>
```json
{
  "collection_id": "string", // A unique identifier, typically a UUID
  "success": boolean
}
```

---

#### Insert Endpoint (`/insert`)
<Frame>
  <img
    src="/images/postman-images/Insert-response.png"
    alt="Postmandocs"
  />
</Frame>
Adds resources to a collection. The response confirms success and tracks token usage.

```json
{
  "success": boolean,
  "tokens": integer
}
```

---

#### Query Endpoint (`/query`)

Fetches information from a collection based on a query. The response contains the result, token usage, and success status.
<Frame>
  <img
    src="/images/postman-images/Query-response.png"
    alt="Postmandocs"
  />
</Frame>
```json
{
  "success": boolean,
  "response": "string",
  "tokens": integer
}
```

---

#### Delete Endpoint (`/delete`)

Removes a collection and returns the operation status with a message.

```json
{
  "success": boolean,
  "message": "string"
}
```

---

#### Categorize Endpoint (`/category`)

Categorizes data according to rules or schemas. The response provides the assigned label, token usage, and success status.

```json
{
  "response": // This structure is defined by the schema,
  "tokens": integer,
  "success": boolean
}

```

